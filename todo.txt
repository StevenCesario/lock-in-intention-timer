* "Anti-letters Firewall" CHECK
* localStorage implementation CHECK
* Reset button (with a `confirm`!) CHECK
* Refine the behavior of the Reset Button and the Pause Button CHECK

* Allow the user to set the intention connected to the timer: CHECK
    * Lock the Intention Input upon clickin "Lock In" CHECK
    * Change "What is your intention for these X minutes?" to "Good Luck! 🌱✨" upon clicking CHECK
    * localStorage implementation for the Intention CHECK

Core functionality:
* What should happen when the timer reaches 00:00? CHECK

Bugs:
* Don't allow an empty intention CHECK
* At least 3 characters for the intention CHECK
* Upgrade the intention validation rule to at least 5 *letters* CHECK
    Digits validation:
    * Preventing more than two numbers on either side of the colon? CHECK
    * Do we parse or validate the digits first? CHECK
    * Move `if (StateBuffer.totalSeconds <= 0) return;` to Validator.validateDigits? CHECK
* Pressing "Lock In" with an invalid timer or intention shouldn't reveal the Reset Button CHECK
* Entering a valid intention has revealed some initialization logic flaws:
    * The Start Button shouldn't say "Lock In", it should say "Continue" CHECK
    * The Reset button should be visible! CHECK
* classList.toggle introduced a new bug: The Reset button is now fading in and out of existence at random. It needs strict remove and add CHECK
* The timer is not ticking when we're in another tab??????? CHECK (Hopefully! For now until a user says otherwise haha!)

UX improvements:
* Have everything else not "jump up" and cause visual whiplash as the Reset timer is shown CHECK
* Remove the visual whiplash that has been re-introduced with the error message haha CHECK
* If there is no blur on the timeDisplay and the user hits Enter again, start the timer CHECK
* timeDisplay.blur(); Do the opposite of this; Add focus to ther timer upon initialization CHECK (but on intentionInput and not timeDisplay haha, see PR #14)
* Warning message: "It is recommended to keep your focus sessions to 60 minutes or less per session" if minutes is > 60 CHECK
* Should we spell out the rule? Or is "Please enter a valid intention" enough? CHECK
* Have longer intentions not "clip" CHECK
* Make it even more obvious that the digits are editable. Different colors behind it?

UX polishes:
* Make the two buttons the same width regardless what mode we're in
* Smooth transitions when the text is changed and the second button is shown
* Implement a "countdown to focus" à la "3, 2, 1.. GO"

Refactoring:
* Initialization; see comment in PR #12